# OmniDev Frontend

This is the Next.js frontend for OmniDev.

## Quickstart

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.

For full setup (including backend + env vars), see the root `README.md`.
