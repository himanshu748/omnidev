fastapi>=0.128.0
uvicorn[standard]
pydantic-settings
google-genai>=1.0.0
boto3
playwright
playwright-stealth
httpx
# OmniDev still uses the v1 FastMCP API. MCP 2 removes those import paths,
# so keep fresh installs on the maintained v1 line until the migration lands.
mcp>=1.28,<2
ipinfo
python-multipart
pytest
pytest-asyncio
moto
numpy
pypdf
# On-device OCR for images and scanned PDFs. macOS only: these cannot
# build on Linux, and CI runs the backend suite on Ubuntu.
pyobjc-framework-Vision; sys_platform == "darwin"
pyobjc-framework-Quartz; sys_platform == "darwin"
